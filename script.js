const minimumAge = function (entreeAge) {
    if (entreeAge >= 18) {
        return "true"
    }
    return "false"
};
const welcomAge = minimumAge(20);
console.log(welcomAge);

const minAge = function (adultAge) {
    if (minimumAge = true) {
        return "Hello there"
    }
    return "Hey kiddo"
};
console.log();
